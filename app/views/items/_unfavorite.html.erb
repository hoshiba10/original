<%= form_for(current_user.favorites.find_by(:item_id => item.id),
             html: { method: :delete },
             remote: true) do |f| %>
　<%= f.hidden_field :item_id, :value => item.id %>
  <%= f.submit "お気に入り解除", class: "btn btn-info btn-xs" %>
<% end %>